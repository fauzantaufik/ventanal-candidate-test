# mobile/CLAUDE.md

Expo React Native (bonus/stretch).

## Commands

```bash
pnpm --filter mobile lint   # Expo lint
```

## Structure

- Tabs: `app/(tabs)/index.tsx` (home), `app/business/[slug].tsx` (detail + reviews stub)
